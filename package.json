{
  "name": "android_applikationen_hexserve",
  "version": "1.0.0",
  "description": "Static file server with TypeScript and Express",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch src --exec \"npm run build && npm start\" -e ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "static-file-server",
    "express",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.7",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}