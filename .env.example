# Server Configuration

# Port number for the server (default: 3000)
PORT=3000

# Host address for the server (default: 0.0.0.0)
# Use 0.0.0.0 to listen on all network interfaces
# Use localhost or 127.0.0.1 for local access only
HOST=0.0.0.0

# Directory to serve files from (default: ./public)
# Can be absolute or relative path
SERVE_DIR=./public

# URL prefix for accessing files (default: /latest)
# Files will be accessible at http://host:port/latest/filename
# Should start with / (will be added automatically if missing)
URL_PREFIX=/latest

# Riot API Proxy Configuration

# Your Riot Games API Key (required for proxy functionality)
# Get your key at: https://developer.riotgames.com/
RIOT_API_KEY=RGAPI-your-key-here

# Riot API base URL (default: https://euw1.api.riotgames.com)
# This is the default base URL used when no ?requestBasePath parameter is provided
# 
# Available Regional URLs (for Match-v5, Account-v1):
#   - europe, americas, asia, sea
# Available Platform URLs (for other APIs):
#   - euw1, eun1, na1, kr, br1, jp1, la1, la2, oc1, tr1, ru, ph2, sg2, th2, tw2, vn2
#
# Note: You can override this on a per-request basis using the ?requestBasePath query parameter
# Example: /riot-api/lol/match/v5/matches/EUW1_123?requestBasePath=https://europe.api.riotgames.com
RIOT_API_BASE_URL=https://euw1.api.riotgames.com

# URL prefix for the proxy endpoint (default: /riot-api)
# API calls will be accessible at http://host:port/riot-api/lol/summoner/v4/...
PROXY_PREFIX=/riot-api

# Logging Configuration

# Log level (default: info)
# Available levels: error, warn, info, debug
# Higher levels include all lower levels (e.g., info includes warn and error)
LOG_LEVEL=info

# Environment (default: development)
# Used in log metadata to distinguish between environments
NODE_ENV=development
